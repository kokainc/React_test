import React, { useState } from 'react';
import GetPrice from './GetPrice';
import {SetPrice} from './GetList';
import Hidebar from '../Hidebar';





export default function Header() {
    const amount = useState(2);
    
}
